const initailStateValue=[]

const userReducers=(state=initailStateValue,action)=>{
     
    switch (action.type) {

        default:{
            return state
        }
     }
}
export default userReducers